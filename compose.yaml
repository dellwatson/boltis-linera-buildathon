services:
  app:
    build: .
    ports:
      - "5173:5173" # Web app
      - "8080:8080" # faucet
      - "9000:9000" # local GraphQL service (linera service)
      - "9001:9001" # shard proxy
      - "13001:13001" # shard
    volumes:
      - .:/build
